def process_list(lst):
    # Sort the list
    sorted_lst = sorted(lst)
    return sorted_lst

# Test cases
test_cases = [
    [],             # Empty list
    [1],            # List with one element
    [7, 7, 7, 7],   # List with all identical elements
    [-5, -1, -3, -2, -4]  # List with negative numbers
]

# Iterate through test cases and print results
for idx, test_case in enumerate(test_cases):
    print(f"Test Case {idx + 1}:")Input: []
    print(f"Input: {test_case}")
    result = process_list(test_case)
    print(f"Expected Output: {result}")
    print()
OUT PUT:
Test case 1:1
input:[]
Expected Output: []

